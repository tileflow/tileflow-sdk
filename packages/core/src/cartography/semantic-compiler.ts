/** Internal compiler identity. Authoring maps never select or version this implementation. */
export const tileflowSemanticCompilerIdentity = Object.freeze({
  name: 'tileflow-semantic',
  version: 1,
} as const);
